package com.untrained.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Background & Surface ─────────────────────────────────────────────────────
val Background = Color(0xFF050505)
val Surface = Color(0xFF131313)
val SurfaceVariant = Color(0xFF1A1A1A)
val Border = Color(0xFF242424)
val BorderMuted = Color(0xFF1C1C1C)

// ── Text ─────────────────────────────────────────────────────────────────────
val TextPrimary = Color(0xFFF4F4F3)
val TextMuted = Color(0xFF8A8A86)
val TextDim = Color(0xFF5D5D5A)
val TextInactive = Color(0xFF3D3D3A)

// ── Accent (Lime) ─────────────────────────────────────────────────────────────
val Accent = Color(0xFFC8FF00)
val AccentDark = Color(0xFF9ACC00)

// ── Semantic Colors ───────────────────────────────────────────────────────────
val Danger = Color(0xFFFF4423)
val DangerDark = Color(0xFFCC3319)
val Warning = Color(0xFFFFB02E)
val Success = Color(0xFF22CC66)

// ── Neutrals ─────────────────────────────────────────────────────────────────
val White = Color(0xFFFFFFFF)
val Black = Color(0xFF000000)

// ── Tab / Nav ─────────────────────────────────────────────────────────────────
val TabInactive = Color(0xFF5D5D5A)
val TabActive = Accent

// ── Aliases used across screens ───────────────────────────────────────────────
val Text2 = Color(0xFFC9C9C7)
val Line = Color(0xFF242424)
val Line2 = Color(0xFF2E2E2E)
val Muted = Color(0xFF8A8A86)
val Muted2 = Color(0xFF5D5D5A)
